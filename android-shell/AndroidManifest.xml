<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.dangish.evogent"
    android:versionCode="3"
    android:versionName="0.3.0">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Needed only for Evogent's replacement digest on Android 13+. MainActivity asks once,
         after notification-listener access exists; denial always leaves originals untouched. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Bootstrap the on-device Termux stack (server + scheduler) at boot via a RUN_COMMAND
         intent, so no separate Termux:Boot addon is required. Termux honors this because its
         config sets allow-external-apps=true. -->
    <uses-permission android:name="com.termux.permission.RUN_COMMAND" />

    <!-- A launcher needs to see every app that has a launcher entry, and resolve YouTube. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <package android:name="com.google.android.youtube" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:theme="@android:style/Theme.Material.NoActionBar"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- The Pixel's assistant gesture replaces Gemini with Evogent's authenticated Add
             Message form. Release provisioning assigns and verifies HOME and ASSISTANT
             transactionally after proving the APK, and restores prior holders on rollback;
             no hotword or microphone permission is requested. -->
        <activity
            android:name=".EvogentAssistantActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:finishOnTaskLaunch="true"
            android:noHistory="true"
            android:stateNotNeeded="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.Material.NoActionBar"
            android:windowSoftInputMode="adjustResize" />

        <service
            android:name=".EvogentVoiceInteractionService"
            android:exported="true"
            android:label="@string/assistant_name"
            android:permission="android.permission.BIND_VOICE_INTERACTION">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
        </service>

        <service
            android:name=".EvogentVoiceInteractionSessionService"
            android:exported="false"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />

        <!-- Required as a concrete metadata target even though Android 12+ ignores the
             recognitionService field for this text-first assistant flow. -->
        <service
            android:name=".EvogentRecognitionService"
            android:exported="true"
            android:permission="android.permission.BIND_SPEECH_RECOGNITION_SERVICE">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
        </service>

        <activity
            android:name=".AppDrawerActivity"
            android:exported="false"
            android:label="Apps"
            android:theme="@android:style/Theme.Material.NoActionBar" />

        <!-- Share target: the background browse shares a YouTube video here to
             capture its real watch URL via pure computer use (no API). -->
        <activity
            android:name=".ShareReceiverActivity"
            android:exported="true"
            android:label="Evogent"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Local notification curation. BIND_NOTIFICATION_LISTENER_SERVICE is system-enforced;
             the service ignores its own digest, strips known secrets before authenticated
             loopback ingestion, and never changes an original without an exact durable receipt.
             Listener access is granted during device setup. -->
        <service
            android:name=".EvogentNotificationListenerService"
            android:exported="true"
            android:label="Evogent notification curation"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Migration shim only: old APK versions may have left one BrowseAlarmReceiver
             PendingIntent behind. The inert receiver cancels it and never launches an app. -->
        <receiver
            android:name=".BrowseAlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="net.dangish.evogent.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <service
            android:name=".EvogentAccessibilityService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

    </application>
</manifest>
